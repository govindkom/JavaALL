package Programss;

import java.util.Arrays;
import java.util.Collections;

public class SortingArrayEle {

	public static void main(String[] args) {
		// SortingArrayEle====> Using Arrays.parallelSort() Methos
		/* Approach 1
		int a[]= {30,50,20,10,60};
		
		System.out.println("arrays before sort:"+Arrays.toString(a));
		Arrays.parallelSort(a);
		System.out.println("arrays after sort:"+Arrays.toString(a));
		
		// Approach 2==> Using Arrays.sort()
		int a[]={30,50,20,10,60};
		System.out.println("arrays before sort:"+Arrays.toString(a));
		Arrays.sort(a);
		System.out.println("arrays after sort:"+Arrays.toString(a));
		
		// Approach 3===> using Reverse Descending order
		Object a[]={30,50,20,10,60};
		System.out.println("arrays before sort:"+Arrays.toString(a));
		Arrays.sort(Collections.reverseOrder());*/
	}

}
